---
description: Commit (and optionally push) using the git-butler agent.
---
Use the git-butler agent to:
- show git status + diff --stat
- summarize changes
- generate one commit message
- commit
If I included the word "push", then push. If I said "push to main", push to main.
